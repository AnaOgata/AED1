#include <stdio.h>

/*Qual o invariante do processo iterativo da função soma no Exercício 1.1.1?*/

/*A função abaixo recebe um valor n >= 1 e um vetor v e retorna a soma de todos os elementos de v[0...n-1]*/
int soma(int n, int v[])
{
    int i, x = 0;
        for (i = 0; i < n; i++) x += v[i]; /*x é a soma de x e v[0...j-1]*/
        return x;
}
